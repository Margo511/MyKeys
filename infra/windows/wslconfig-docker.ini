# Copiar como %UserProfile%\.wslconfig en equipos Windows que ejecuten Docker Engine
# dentro de WSL. Los servicios systemd no mantienen viva por si solos la instancia.
[wsl2]
vmIdleTimeout=-1

[general]
instanceIdleTimeout=-1
